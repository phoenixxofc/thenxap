{
  "name": "arena-dash",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -ws",
    "dev": "npm run dev -ws",
    "dev:web": "npm run dev -w @arena-dash/web"
  }
}
