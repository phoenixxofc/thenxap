{
  "name": "arena-dash",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "dev:web": "npm run dev -w @arena-dash/web"
  },
  "devDependencies": {
    "turbo": "latest"
  }
}
